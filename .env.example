# Base URL of the Spring Boot REST API.
# Optional: if unset/blank the app falls back to http://localhost:8082
# (see src/lib/api/baseUrl.ts). Must be reachable from the browser since
# API calls run client-side from React islands.
PUBLIC_API_BASE_URL=http://localhost:8082
